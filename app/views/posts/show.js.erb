$('#products').append('<%= j render('comments/ordered') %>');
<% if @ordered.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@ordered) %>');
<% else %>
  $('.pagination').remove();
<% end %>