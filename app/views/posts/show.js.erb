$('#products').append('<%= j render('comments/answers_feed') %>');
<% if @comments.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@comments) %>');
<% else %>
  $('.pagination').remove();
<% end %>