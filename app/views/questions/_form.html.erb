<%= simple_form_for [@comment, Question.new] do |f| %>
<%= f.hidden_field :user_id, :value => current_user.id %>

  <%= f.input :body, as: :text, input_html: { :class => "hello3" }, label: false, placeholder: "Ask Question" %>

<%= f.submit "Ask", class: "button5" %>

<% end %>

