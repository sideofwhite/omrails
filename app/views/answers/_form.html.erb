<%= simple_form_for [@question, @answer, Answer.new] do |f| %>
 <p>
<div>
  
  <%= f.input :body, as: :text, input_html: { rows: "10" } %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  </div>

   
 </p>
 <p><%= f.submit "Answer", class: "btn btn-primary" %></p>
 <% end %>
